[package]
name = "cuin-analyzer"
version = "0.0.15"
edition = "2024"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"

[lib]
crate-type = ["cdylib", "rlib"]
path = "src/lib.rs"

[dependencies]
dashmap = "6.1"
ignore = "0.4"
napi = { version = "3", optional = true }
napi-derive = { version = "3", features = ["type-def"], optional = true }
oxc = { version = "0.94.0", default-features = false, features = ["ast_visit"] }
oxc_resolver = "11.9.0"
oxc_syntax = "0.94.0"
rayon = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sonic-rs = "0.3"

[build-dependencies]
napi-build = "2"

[dev-dependencies]
tempfile = "3"

[features]
default = ["napi"]
napi = ["dep:napi", "dep:napi-derive"]
test-helpers = []
