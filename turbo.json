{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["tsconfig.base.json"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "bindings/**", ".output/**"]
    },
    "@ikuma-t/cuin#build": {
      "dependsOn": ["@cuin/ui#build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "typecheck": {},
    "test": {},
    "//#check": {},
    "//#fix": {
      "cache": false
    }
  }
}
