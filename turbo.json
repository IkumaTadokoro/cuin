{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["bindings/**"]
    },
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
